specVersion: "v1beta"
serviceId: "crud"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "crud-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
